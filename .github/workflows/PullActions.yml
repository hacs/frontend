name: "Pull"

on:
  pull_request:
    branches:
      - master

jobs:
  build_frontend:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Build
        run: |
          npm install
          npm run build
